import { CalculaIMCPipe } from './calcula-imc-pipe';

describe('CalculaIMCPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculaIMCPipe();
    expect(pipe).toBeTruthy();
  });
});
